# OS / editor junk
.DS_Store
*~
*.swp
*.swo

# IDE project folders (auto-created when opening this repo — never commit)
.idea/
.vscode/

# AI assistants / MCP local data (per-machine, never commit; matches at any depth)
.claude/
.serena/
.codex/
.junie/
.cursor/
.windsurf/
.aider*
.mcp.json

# per-machine override files (kept local, never committed)
ghostty/local
nvim/lua/local.lua

# OS / editor junk
.DS_Store
*~
*.swp
*.swo

# IDE project folders (auto-created when opening this repo — never commit)
.idea/
.vscode/

# local Claude project data (not shared across machines)
nvim/.claude/

# per-machine override files (kept local, never committed)
ghostty/local
nvim/lua/local.lua
